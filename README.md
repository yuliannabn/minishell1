# minishell1

A small shell made in C.

## FEATURES

* cd / change directory (with -, ~ support)
* Execute binaries (supports PATH in env) 
* exit function
* setenv, unsetenv

## compilation:
> make re
